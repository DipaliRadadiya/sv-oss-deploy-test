# sv-oss-deploy-test

A tiny repository for testing ServerAvatar OSS git deploys. Safe to delete.

- `main`: a PHP site. `index.php` prints the live `VERSION`, the PHP version and whether `composer install` ran.
- `node`: a Node app. `npm run build` writes `BUILD`, `npm start` serves on `$PORT`.
